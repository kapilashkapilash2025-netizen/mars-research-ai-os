# Twin Schema Versioning

The gateway supports semantic schema `v1`. Unsupported versions fail explicitly; future schema versions
must provide backward-compatible readers and migration tests before registry inclusion.
