# Reproducible AI OS Image Foundation

The image manifest defines a pinned Debian-minimal deployment contract, architecture, package set,
configuration hash and build seed. It is reproducible metadata, not a generated boot image.
