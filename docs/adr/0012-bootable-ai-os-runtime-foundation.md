# ADR 0012: Bootable AI OS Runtime Foundation

Use a Linux service runtime foundation before attempting a custom kernel. Safety service availability is a
boot prerequisite; missing critical services lead to safe mode. This is a simulation deployment contract,
not a bootable rover image.
