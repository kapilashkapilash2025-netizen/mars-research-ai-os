# Persistent State Layout

Persistent data must separate immutable audit logs, Twin snapshots, fault history, recovery records and
boot/update records. Power-loss-safe storage selection and migration implementation remain future work.
