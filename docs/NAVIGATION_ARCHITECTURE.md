# Navigation Planning Foundation

Navigation is intent-only. A deterministic static World Model flows through route planning, constraints
and risk estimation to immutable `NavigationIntent`. It has no HAL, motor-command or Twin-mutation API.
